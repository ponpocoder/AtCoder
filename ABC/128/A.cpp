#include <iostream>

using namespace std;
using ll = long long;

#define rep(i, n) for (int i = 0; i < (n); i++)

int main() {
  int a, p;
  cin >> a >> p;
  int res = (a*3+p)/2;
  cout << res << endl;
}
